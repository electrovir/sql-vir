import {DatabaseConnectionInfo, DatabaseUser} from './database-connection';

export function createDatabase(inputDatabase: DatabaseConnectionInfo): boolean {
    return false;
}

export function deleteDatabase(inputDatabase: DatabaseConnectionInfo): boolean {
    return false;
}

export function createUser(inputUser: DatabaseConnectionInfo): boolean {
    return false;
}

export function grantUserPermission(databaseInput: DatabaseConnectionInfo): boolean {
    return false;
}
